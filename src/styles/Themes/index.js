/* Colors */
const colors = {
  white: '#fff',
  black: '#000',
  red: '#ff040d',
  transparent: 'transparent',
  yellow: '#ffee17',
};
const fonts = {
  weight: {
    light: 300,
    normal: 400,
    bold: 600,
    bolder: 700,
  },
};

const theme = {
  main: {
    colors: {
      ...colors,
    },
    fonts: {
      ...fonts,
    }
  },
};

export default theme;